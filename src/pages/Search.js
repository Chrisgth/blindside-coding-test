import { useAuth0 } from "@auth0/auth0-react";

const Search = () => {
  return (
    <div className="search">
      <input type="text" />
      <button>search</button>
      <p></p>
    </div>
  );
};

export default Search;
